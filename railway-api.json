{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "preDeployCommand": "php artisan migrate --force --seed && php artisan optimize:clear && php artisan scribe:generate"
  }
}
